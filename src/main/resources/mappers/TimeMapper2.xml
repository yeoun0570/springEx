<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssg.springex.mapper.TimeMapper2">
    <select id="getNow" resultType="string">/* id는 메서드명, resultType은 필드타입*/
        select now()
    </select>
</mapper>

<!--select 태그는 반드시 resultType이나 resultMap이라는 속성을 지정해야 한다.
select문의 결과를 어떤 타입으로 처리할 지에 대한 설정 java.lang.String -->